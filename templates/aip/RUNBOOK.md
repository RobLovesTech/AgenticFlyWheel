Runbook

Flags
- Enumerate runtime flags and defaults.

Local Validation
- Run `verification.commands` from `CHECKLIST.yaml` (update them during planning so they match this repo’s tooling).

Flip/Rollback
- How to flip feature on; how to rollback safely.

Queries
- Example DB queries to verify data writes (if applicable).
